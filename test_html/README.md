# 测试页面说明

本目录用于存放本地接口联调用的临时 HTML 页面和现场验证说明，主要服务于数据库、批量 SQL、旧网页兼容等人工冒烟测试场景。

## 使用约定

- 测试页面只用于本地或现场验证，不作为生产运维界面发布。
- 新增测试页面时，文件名应能体现验证目标，例如 `batch_test.html`。
- 如测试页面依赖固定端口、连接名或接口路径，应在页面内或相邻文档中写明前置条件，避免误判接口异常。
